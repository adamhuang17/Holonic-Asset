pre-commit:
  commands:
    backend-format:
      root: "backend/"
      glob: "*.go"
      run: golangci-lint fmt {staged_files}
      stage_fixed: true

    backend-lint:
      root: "backend/"
      glob: "*.go"
      run: golangci-lint run